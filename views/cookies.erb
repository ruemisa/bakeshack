<section class="product-container">
    
        <% products = $cookies %>
        <% products.each do |cookie| %>
    <div class="products">  
        <h2><%= cookie.name %> </h2>
        <img class="product-img" src=<%=cookie.images %> >
            <%  description = cookie.desc %>
            <%  description.each do |desc| %>
            <ul> 
                <li> <%= desc %> </li>                       
            </ul>
                <% end %>
        <p> $<%= cookie.price %> </p>
        <form method="PATCH" action="/cart" autocomplete="off" class="cart-form">
            <label> Qty: </label>
            <input type="text" name="quantity" required>
            <a href="">
                <img src="/images/icons/025-shopping-bag.png">
            </a>
        </form>
    </div>  
        <% end %>
</section>